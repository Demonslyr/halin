stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: atriarch-halin:v1.0.${CICD_EXECUTION_SEQUENCE}
      pushRemote: true
      registry: http://nexus.drinkpoint.me/repository/atriarch-docker/
timeout: 60
notification:
  recipients:
  - recipient: dan@mail.atriarch.systems
    notifier: c-xc9r9:n-g2gcc
  condition:
  - Success
